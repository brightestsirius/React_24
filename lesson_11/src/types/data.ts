interface IPost {
    id: number;
    userId: number;
    title: string;
    body: string;
}

interface IUsers {
    id: number;
    name: ``
}

interface ITodos {
    id: number;
    title: ``
}

export type { IPost, IUsers, ITodos }